function multiplicador(a: number, b: number) {
    const resultado: number = a * b;
    console.log(`o resultado de ${a} * ${b} é igual ${resultado}`);

    return resultado;
}

multiplicador(2,3);
multiplicador(4,6);


function saudacao(nome: string) {
    nome: nome;
    console.log(`Olá ${nome}`);

    return nome;
}

saudacao('yuri');


